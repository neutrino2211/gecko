---
title: Fn (trait)
description: Fn<A, R> - Callable types (multiple calls allowed).
---

```gecko
trait Fn<A, R>
```

Fn<A, R> - Callable types (multiple calls allowed).

## Required Methods

### call

```gecko
func call(self: void, arg: A): R
```

