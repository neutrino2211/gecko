---
title: Clone (trait)
description: Clone - Trait for types that can be explicitly duplicated.
---

```gecko
trait Clone
```

Clone - Trait for types that can be explicitly duplicated.
Call `.clone()` to create a deep copy of a value.

## Required Methods

### clone

```gecko
func clone(self: void): Self
```

