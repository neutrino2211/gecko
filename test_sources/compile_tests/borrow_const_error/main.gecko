package main
import std.core.traits use { BorrowMut }
import std.memory.borrow_cell use { BorrowCell }
external func main(): int32 {
 const cell: BorrowCell<int32> = BorrowCell<int32>::new(1)
 let writer = @borrow_mut(cell)
 return 0
}
