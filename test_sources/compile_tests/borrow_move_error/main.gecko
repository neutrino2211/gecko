package main
import std.memory.borrow_cell use { BorrowCell }
external func main(): int32 {
 let cell: BorrowCell<int32> = BorrowCell<int32>::new(1)
 let writer = cell.borrow_mut()
 let moved = writer
 writer.set(2)
 return 0
}
