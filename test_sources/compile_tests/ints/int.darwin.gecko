package main

declare external variardic func printf(format: string)

func main (): int {
  const hello: string = &"A string"

  printf(&"A CONST %d\n", 90)
  
  printf(&"A let %s\n", &"HH")

  printf(hello)

  return 0
}