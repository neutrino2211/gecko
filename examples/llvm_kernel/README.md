# LLVM Kernel (Historical)

This example targets the deprecated LLVM backend, which is unavailable in the current compiler. It is retained as a historical reference and does not build. For a maintained kernel example, use [`../hello_kernel/`](../hello_kernel/).
