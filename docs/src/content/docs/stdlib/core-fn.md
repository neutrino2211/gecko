---
title: Fn (trait)
description: Fn<A, R> - Trait for callable types (can be called multiple times).
---

```ts
trait Fn<A, R>
```

Fn<A, R> - Trait for callable types (can be called multiple times).

## Required Methods

### call

```ts
func call(self: void, arg: A): R
```

