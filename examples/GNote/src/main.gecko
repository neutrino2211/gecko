// GNote - A Gecko executable

import utils.fs use { File }
import std.option use { Option }

declare external func puts(s: string): int32

external func main(): Option<int32> {
    let file: File = try File::open("no_exist", "r")

    puts("Hello from GNote!")
    return Option<int32>::some(0)
}
