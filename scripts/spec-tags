#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

MODE="${1:-audit}"

case "$MODE" in
  audit|apply|check) ;;
  *)
    echo "Usage: scripts/spec-tags [audit|apply|check]"
    exit 2
    ;;
esac

go run ./tools/spec_tags "$MODE"
