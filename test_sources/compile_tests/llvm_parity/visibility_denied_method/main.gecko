package llvm_parity_visibility_denied_method

import provider use { Counter, make_counter }

external func main(): int32 {
    let c: Counter = make_counter(12)
    c.hidden()
    return c.hidden()
}
