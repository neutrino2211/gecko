// spec: spec/types.md, spec/functions.md, spec/modules.md, spec/control-flow.md, spec/memory.md

package type_check

class Data {
    let value: int
}

func main(): int {
    let a: Data
    a.value = 42

    let b: Data = a
    let x: int = a.value

    return x + b.value
}
